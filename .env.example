# Supabase配置（如果使用Supabase）
SUPABASE_URL=...
SUPABASE_ANON_KEY=...           # 给普通 JWT 验证
SUPABASE_SERVICE_KEY=...        # 需要越权操作时
SUPABASE_JWT_SECRET=...         # 项目设置 → API → JWT secret

# 数据库配置（直连PostgreSQL）
DB_HOST=localhost
DB_PORT=5432
DB_NAME=profile_matcher
DB_USER=postgres
DB_PASSWORD=your_password